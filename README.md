# ResonAI - AI Audio Upscaler


ResonAI — это веб-приложение, использующее нейронные сети для улучшения качества аудиофайлов, превращая их в Hi-Res звук (24 бит / 48 кГц или даже 192 кГц).

---

## 🚀 Возможности

- **AI-улучшение качества аудио**: Поднимите разрешение ваших аудиозаписей.
- **Поддержка различных форматов**: Обработка `.mp3`, `.wav`, `.flac`.
- **Hi-Res аудио на выходе**: Получайте файлы в формате 24 бит / 48 кГц FLAC или даже 192 кГц (для Audiophile модели).
- **Различные AI-модели**: Выбор между "Standard", "Audiophile" и "Deep Restore" для разных сценариев улучшения.
- **Простой веб-интерфейс**: Загрузка аудиофайлов через браузер, отображение прогресса и скачивание улучшенного результата.
- **Обработка потоком**: Эффективная работа с большими аудиофайлами без переполнения памяти благодаря чанкованию.

---

## 🎯 Цель проекта

Основная цель - создать инструмент, который, способен "достраивать" утерянные детали в аудиосигнале, повышая его частоту дискретизации и битность, и тем самым улучшая слушательский опыт. Это не простое пересэмплирование, а интеллектуальное восстановление с помощью глубоких нейронных сетей.

---

## 🛠 Установка

1.  **Клонируйте репозиторий:**
    ```bash
    git clone https://github.com/Parad1st/ResonAI.git 
    cd ResonAI
    ```

2.  **Создайте и активируйте виртуальное окружение (рекомендуется):**
    ```bash
    python3 -m venv venv
    source venv/bin/activate # Для Linux/macOS
    # venv\Scripts\activate # Для Windows
    ```

3.  **Установите зависимости:**
    ```bash
    pip install -r requirements.txt
    ```

4.  **Установите системные зависимости (FFmpeg и libsndfile для torchaudio):**
    ```bash
    sudo apt update
    sudo apt install ffmpeg libsndfile1 # Для Ubuntu/Debian
    # brew install ffmpeg libsndfile # Для macOS с Homebrew
    # Другие ОС - следуйте инструкциям для установки FFmpeg и libsndfile
    ```

---

## 📚 Обучение моделей

Для обучения нейронной сети вам потребуются высококачественные аудиофайлы. Скрипты обучения сами создают пары "низкое качество" и "высокое качество" на лету.

### 1. Подготовьте данные для обучения

Поместите ваши аудиофайлы (FLAC, WAV) в папку и укажите путь к ней. Рекомендуется использовать файлы с частотой дискретизации 48 кГц и выше (для обучения Audiophile Model нужны файлы 192кГц).

### 2. Запуск обучения

В зависимости от желаемой модели, запустите соответствующий скрипт:

-   **Standard Model (48kHz / 24bit)**:
    ```bash
    python train.py
    ```
    (Создаст `upscale_model_standard.pth`)

-   **Audiophile Model (192kHz Beta)**:
    ```bash
    python train_192k.py
    ```
    (Создаст `model_192k_beta.pth`)

-   **Deep Restore Model (Beta)**:
    ```bash
    python train_restore.py
    ```
    (Создаст `model_restore_beta.pth`)

> **Примечание:** Обучение может занять много времени и требует мощного GPU с достаточным объемом VRAM. При необходимости уменьшайте `BATCH_SIZE` в файлах `train_*.py`.

---

## 🌐 Запуск веб-приложения

После того как вы обучили хотя бы одну модель (и получили соответствующий `.pth` файл), вы можете запустить веб-приложение:

```bash
python main.py
